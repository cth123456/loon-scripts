#!name=AgentRouter 自动签到
#!desc=v1.1.0 · agentrouter.org 每日自动签到：用账号密码完成一次登录即签到，并回查个人日志确认「签到成功」。安装后请在下方填入账号（AGENTROUTER_ACCOUNT=邮箱#密码；多账号用 AGENTROUTER_ACCOUNTS 填 JSON 数组）。签到默认每天 09:00 运行；如想在一天内多个时间点签到，填 AGENTROUTER_RUN_HOURS（如 9,15,21）即可，脚本会在每小时被叫起、只在匹配的小时真正执行。另外每周一会自动检查上游 Python 脚本是否有未移植的新提交并提醒。
#!author=cth123456
#!homepage=https://github.com/cth123456/loon-scripts
#!icon=https://raw.githubusercontent.com/Koolson/Qure/master/IconSet/Color/Proxy.png
#!input = AGENTROUTER_ACCOUNT
#!input = AGENTROUTER_ACCOUNTS
#!input = AGENTROUTER_BASE_URL
#!input = AGENTROUTER_RUN_HOURS

[Script]
cron "0 * * * *" script-path=https://raw.githubusercontent.com/cth123456/loon-scripts/main/agentrouter-checkin.js, tag=AgentRouter签到, enable=true, timeout=60
cron "0 10 * * 1" script-path=https://raw.githubusercontent.com/cth123456/loon-scripts/main/upstream-watch.js, tag=AgentRouter上游检查, enable=true, timeout=60
